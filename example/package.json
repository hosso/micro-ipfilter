{
  "name": "micro-ipfilter-example",
  "version": "0.1.0",
  "private": true,
  "description": "Example of micro-ipfilter",
  "main": "index.js",
  "scripts": {
    "start": "micro",
    "deploy": "now && now alias && now rm micro-ipfilter-example --safe --yes"
  },
  "dependencies": {
    "micro": "^9.3.3",
    "micro-ipfilter": "hosso/micro-ipfilter"
  },
  "devDependencies": {
    "now": "^13.0.4"
  }
}
